import {Component} from '@angular/core';

@Component({
    selector: 'app-header',
    templateUrl: './header.component.html',
    styles:[
        `div{
            text-align:center;
            color:teal
        }`
    ]
})

export class HeaderComponent {
    fontSize = '10px'
}